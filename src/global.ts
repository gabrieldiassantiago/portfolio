import { createGlobalStyle } from 'styled-components';

export const Estilos = createGlobalStyle`
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  font-display: swap;
  outline: none;
  overflow: hidden;
  width: 100%;
`